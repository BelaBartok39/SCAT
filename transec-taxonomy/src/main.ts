/**
 * TRANSEC Taxonomy — bootstrap.
 * Wires store → views. Populated as phases land.
 */

const app = document.getElementById('app')!;
app.innerHTML = '<p style="color:#9ca3af;font-family:monospace;padding:2rem">TRANSEC taxonomy — scaffold OK</p>';
